## 1. 重试与异常边界

- [x] 1.1 扩展 `RetryEngine` 对 subprocess 超时、连接异常和显式 `RetryableError` 的分类支持，保留原始异常链
- [x] 1.2 为 CLI 非零返回、授权失败、参数错误和响应契约错误建立不可重试的明确异常路径
- [x] 1.3 补充重试与最终失败的集成日志字段和单元测试，确认敏感参数脱敏

## 2. 妙记真实读取

- [ ] 2.1 为 `minutes +detail` 创建按 minute token 隔离的稳定 `--output-dir`
- [ ] 2.2 校验 CLI 返回逐字稿路径位于预期目录，并处理缺失、空文件、编码和不可读错误
- [ ] 2.3 为妙记搜索和详情入口接入正确的重试与失败日志边界
- [ ] 2.4 增加 subprocess mock 测试覆盖输出目录、路径安全、逐字稿保留和失败场景

## 3. Base、Task、Docs 响应可靠性

- [ ] 3.1 Base 更新/查询/列表失败时停止吞错，区分空查询与 CLI 执行失败
- [ ] 3.2 Base 创建和更新响应校验 record ID、状态和可恢复信息
- [ ] 3.3 Task 创建响应校验非空 task GUID，并为查询和状态更新接入可靠性边界
- [ ] 3.4 Docs 发布响应校验非空 doc token，查询失败明确抛错并修正稳定检索标识
- [ ] 3.5 增加上述三类 adapter 的成功、空结果、非零返回、非法 JSON、空 ID 测试

## 4. 卡片出向可靠性

- [ ] 4.1 将审批、阻断、告警卡片发送统一接入重试和集成日志
- [ ] 4.2 将卡片更新接入重试、开始/成功/失败日志和响应校验
- [ ] 4.3 增加各类卡片发送与更新失败恢复测试

## 5. Aily 编译与副作用幂等

- [ ] 5.1 让 Aily 非法 JSON、缺少候选字段和候选结构错误明确失败，不降级为空候选成功
- [ ] 5.2 调整 Aily 重试边界，避免多分段失败时重复执行已成功分段
- [ ] 5.3 为任务创建增加 candidate 级稳定业务键或创建前查重，覆盖客户端超时后的恢复
- [ ] 5.4 为批量台账写入记录已成功 record ID，重试/恢复时跳过已完成项

## 6. 上线文档与验收

- [ ] 6.1 更新 `DEPLOYMENT.md`，补充 Real 模式失败日志、资源 ID 核验和回滚步骤
- [ ] 6.2 完成 lark-cli `--dry-run` 命令核对及 user/bot 授权检查
- [ ] 6.3 运行集成测试、Phase 3、health check、端到端 demo 和 OpenSpec 严格校验
- [ ] 6.4 完成最小真实飞书端到端验收并记录实际结果（若权限或资源不足，明确保留阻塞项）

## 7. 归档

- [ ] 7.1 逐项核对实现与规格，完成全部可执行任务
- [ ] 7.2 归档 `production-reliability-hardening` 并同步主规格
