# LabMemory 晶研智流 · 完整参赛方案

> **文档性质**：AI 先锋未来人才大赛 2026 复赛完整参赛方案
> **队伍**：溯研 LabTrace
> **方案定位**：嵌入飞书的可信实验决策与记忆系统
> **部署实绩**：HTTPS 公网域名 `https://<your-domain.com>`（阿里云 <SERVER_IP>，可公开访问）
> **数据性质**：所有指标均来自脱敏模拟数据与评测样例，不代表晶泰科技真实业务收益

---

## 一、参赛方案信息卡【必须包含】

| 项目 | 填写内容 |
|---|---|
| **队名** | 溯研 LabTrace |
| **命题** | 晶泰科技命题 · 嵌入飞书的 AI for Science 实验决策可信化（基于"AI + 机器人工作站 + 实验软件"自主实验闭环的会议决策记忆层） |
| **一句话摘要** | LabMemory（晶研智流）是嵌入飞书的**可信实验决策与记忆系统**：把飞书会议中的科学讨论自动编译为可审核、可版本化的实验决策——让每条参数有证据、每次执行用对版本、每个结果反向校正知识。 |
| **成员介绍 & 分工** | **王健俊**（队长 / 产品与可信记忆负责人）：北京航空航天大学网络空间安全学院本科在读。负责定义"主张—证据—版本—状态—适用范围—责任"的可信记忆模型、产品形态与 PRD；负责评测口径设计与"会后整理时间、过期参数拦截率、知识发布率"等业务指标定义。<br><br>**韩广宁**（飞书侧编排与 AI 接入负责人）：负责妙记/事件接入、飞书 Aily MCP 与 Skill 决策编译、飞书卡片/任务/多维表格/知识库联动、幂等/重试/验签等可靠性工程（`feishu-orchestrator` 子系统）；本次完成了 10 个 MCP 工具、5 个 Webhook 事件、38 项集成测试全绿。<br><br>**林俊衡**（自研平台与可信决策系统负责人）：负责平台前后端（FastAPI + React 19 + Vite 6 + SQLAlchemy）、六道质量闸门/参数级版本/5 态知识状态/三值留痕/冲突检测/语义栅栏/混合检索 RAG 等可信决策内核（`labmemory-platform` 子系统）；本次完成三轮对抗审查、修复 ~40+ 条缺陷、归档 16+ 个 OpenSpec change。 |
| **使用的飞书 AI 能力** | ① **飞书 Aily（MCP + Skill）**：会议决策编译器——Aily 通过标准 MCP 协议（SSE 传输）调用 LabMemory 暴露的 10 个可信决策工具（`labmemory_submit_transcript` / `labmemory_submit_extraction` / `labmemory_create_review` / `labmemory_submit_verdict` / `labmemory_issue_version` / `labmemory_preflight_check` / `labmemory_submit_execution` / `labmemory_update_passport` / `labmemory_publish_knowledge` / `labmemory_create_reverify`），将妙记逐字稿编译为结构化候选对象（参数候选/主张/争议/风险/任务/证据锚点），并按 10 步流程（收逐字稿→抽取→建复核→裁决→签版→自检→执行回写→护照更新→知识发布→复验）走完可信决策链路。<br>② **妙记 / 会议智能纪要**：会议逐字稿作为决策编译的原始输入与证据锚点来源；Aily 通过 Skill 把妙记非结构化文本编译为 6 类候选（decision / conclusion / risk / action_item / question / parameter_change）。<br>③ **飞书卡片（交互卡片）**：复核卡、审计阻断卡、失败边界卡、复验提醒卡——支持卡片按钮直接 approve / reject / revise，触发平台六道闸门 + 行动前审计 + 任务状态回写全流程。<br>④ **飞书任务**：决策通过审计后自动创建任务并回写 `feishu_task_guid`，任务状态变更通过 `/api/v1/task/status` 双向同步。<br>⑤ **多维表格 / 飞书知识库**：候选数据落表与知识发布（`labmemory_publish_knowledge` 触发 `knowledge.ready` Webhook → 飞书知识库 + 复验提醒）。<br>⑥ **飞书开放平台（事件订阅 + Webhook + OpenAPI）**：妙记生成事件驱动全流程；平台主动 Webhook 推送 5 类事件（`decision.pending` / `preflight.blocked` / `execution.deviated` / `knowledge.ready` / `reverify.due`），HMAC-SHA256 签名 + 幂等键去重。 |

---

## 二、方案成果展示

### 1. 命题场景描述、问题描述及痛点说明

#### 1.1 真实业务场景（晶泰科技自主实验闭环的"决策真空层"）

晶泰科技已构建 "**AI + 机器人工作站 + 实验室软件（ELN/LIMS）**" 构成的自主实验闭环：AI 生成反应路线、机器人工作站 7×24 小时执行合成、ELN/LIMS 沉淀原始数据。从"自动化执行"角度看，实验链路的"端"已经成熟，但中间存在一个被忽视的真空层——

> **会议中产生的科学判断（温度、催化剂、时间、浓度），能否与下游的实验编号、参数版本、原始结果、任务和责任人稳定对齐？**

我们把这条决策真空层的失败后果概括为一句话：**机器人在跑的是"昨天会上的决定"，但昨天会上说的那个温度，今天已经被另一场会议替换成了新值。**

一条实验决策的真实生命周期如下：

```
┌────────────────────┐
│ 飞书会议（妙记）     │  「我们这次把温度降到 70 度」
└──────────┬─────────┘
           ▼
┌────────────────────┐
│ 人工整理纪要（30 分钟）│  漏掉理由 / 适用范围 / 证据
└──────────┬─────────┘
           ▼
┌────────────────────┐
│ 旧文档 / 旧任务参数   │  「80℃」——已被新会议替代，但系统无感知
└──────────┬─────────┘
           ▼
┌────────────────────┐
│ 机器人执行（机时 + 物料）│ 错误的温度进入真实实验
└──────────┬─────────┘
           ▼
┌────────────────────┐
│ 实验结果             │  转化率 72%（vs 70℃ 的 78%）
└──────────┬─────────┘
           ▼
┌────────────────────┐
│ 报告与原决策割裂       │  「为什么用 80℃？」——翻聊天记录找不回来
└────────────────────┘
        （知识沉淀断裂）
```

#### 1.2 五大核心痛点

| # | 痛点 | 业务影响 |
|---|---|---|
| ① | **会后整理耗时且失真** | 人工从长纪要里提炼"到底决定了什么"，容易遗漏理由、适用范围和证据——而这恰恰是科学决策最关键的部分（PI 一周花在会议整理上的时间约 5-8 小时，估算）。 |
| ② | **旧参数被静默沿用** | 从旧文档/旧任务复制参数时，参数早已被新实验结论替代，系统无感知——**错误温度直接进入机器人实验，浪费的是数小时~一天机时 + 稀有物料 + 数天等待周期**。这是评测中"过期参数拦截率"维度要解的核心问题。 |
| ③ | **失败经验无法复用** | "转化率上升但副产物超标"在传统二元（成功/失败）记录里只能标"成功"，预测偏差不闭环；下次实验还会犯同样的错。 |
| ④ | **AI 输出缺少治理** | Aily 把会上"可以试试 65℃"的试探性表达误识别为最终参数，或错误关联实验编号——直接采信会污染实验记录，甚至产生合规风险。 |
| ⑤ | **静默失败** | 接口失败、证据失效、重复事件、版本错配可能被系统隐藏成"已完成"——乐观的 success 掩盖了真实的失败，最后复盘时找不到根因。 |

#### 1.3 根因分析

> **会议记录（长文本）与实验事实（对象/数值/单位/版本/责任）数据结构不同；知识状态过于粗糙（事实/建议/假设/待验证混在一起）；版本与适用范围缺失；AI 输出缺少治理层。**

这五个根因决定了：必须建立**新的产品形态层**，不能靠改进现有 ELN/LIMS 解决——因为根因在"会议→决策"这一段，不在"执行→记录"那一段。这正是 LabMemory 的产品切入点。

---

### 2. 方案优势 / 创新点

#### 2.1 覆盖范围：完整决策链路，不重做会议摘要，不替代 ELN/LIMS

LabMemory 专注做缺失的"**可信决策记忆层**"：

```
会议纪要 → 参数决策 → 实验执行 → 结果回流 → 知识复用
   ↑                                       ↓
   └──────────── 失败边界卡 / 复验任务 ──────┘
```

#### 2.2 五大核心创新点

| # | 创新点 | 解决什么 |
|---|---|---|
| ① | **会议决策编译器 + AI/规则分工** | Aily 只做语义理解（妙记 → 结构化候选），编号/数值/单位/版本/权限/幂等由确定性规则引擎控制——AI 与规则分工，**AI 不直接决定"什么生效"**。这是与"AI 直接给结论"型方案最本质的差异。 |
| ② | **六道质量闸门** | 候选发布前过 对象门→参数门→证据门→范围门→状态门→责任门；任一失败不得成为生效主张或正式任务。失败原因可定位、可修复、可重审。 |
| ③ | **行动前审计三态** | 实验启动前五项检查（版本/证据/审批/资源/失败边界）→ 通过 / 需确认 / 阻断，**在错误知识进入实验前拦截**，并支持"一键修正为当前版本"——而不是事后复盘。 |
| ④ | **非二元知识状态 5 态** | supported / partially_supported / refuted / replaced / insufficient_evidence——"转化率升但副产物超标"被记为 `partially_supported` + 失败边界卡 + 复验任务，而不是简单"成功"。 |
| ⑤ | **三值留痕 + 参数级版本** | 原始会议原话 / AI 候选值 / 人工确认值分别保存；新版本按 `(experiment_id, parameter_name, scope)` 维度替代旧版本，**未变参数继承不升版**，历史永久可追溯。 |

#### 2.3 Before / After 对比

| 维度 | Before（现状） | After（LabMemory） |
|---|---|---|
| **会后整理** | 人工通读长纪要，30-60 分钟/次，遗漏理由与证据 | Aily 自动编译为结构化候选，复核卡 3 分钟内确认，整理时间降低 **56%**（模拟数据） |
| **参数版本** | 从旧文档复制，过期参数无感知进入实验 | 行动前审计自动比对版本，旧参数任务被**阻断**并支持一键修正为当前版本（拦截率 **100%**，模拟数据） |
| **实验结果** | 二元"成功/失败"，失败经验沉没 | 五态知识状态 + 失败边界卡 + 自动复验任务，偏差闭环 |
| **AI 可信度** | AI 摘要直接采信，"可以试试"可能变成正式参数 | 语义栅栏（试探性表达不生效）+ 六道闸门 + 三值留痕，任何修改有据可查 |
| **知识检索** | 翻聊天记录 / 翻旧文档 | 实验护照：任一结果可回溯到决策与会议原话，任一决策可看到后续影响 |
| **跨会议一致性** | 80℃ v1 和 70℃ v2 谁生效靠人记 | `replaces` 关系自动 supersede，参数级版本链可追溯 |
| **失败闭环** | 失败经验只留在个人笔记里 | 失败边界卡自动生成 + 复验任务自动创建，根因假设显式标注"假设" |
| **AI 与规则边界** | AI 直接决定生效参数 | AI 出候选，规则闸门决定生效；编号/数值/版本/权限/幂等 100% 确定性 |

#### 2.4 预计量化价值（脱敏模拟数据，非真实业务收益）

| 指标 | 原型目标 | 样例结果 | 业务意义 |
|---|---|---|---|
| **过期参数拦截率** | ≥90% | **100%** ✅ | 每次拦截避免一轮机器人机时（数小时~一天）+ 稀有物料 + 数天等待周期 |
| **人工整理时间降低** | ≥50% | **56%** ✅ | PI 一周节省 5-8 小时整理时间 |
| **24h 知识发布率** | ≥90% | **92%** ✅ | 实验结论当日入知识库，新人第二日即可检索 |
| **问答可追溯率** | 100% | **100%** ✅ | 每个答案可回到会议原话与证据（带引用 + 跳转） |
| **人工修改率** | ≤20% | **10%** ✅ | Aily 抽取一次到位，复核修改率低 |
| **冲突检测 Precision / F1** | ≥90% / ≥0.87 | 100% / 0.89 ✅ | 数值冲突无漏报 |
| **实验要素抽取准确率** | ≥90% | 90% ✅ | Aily 抽取与人工标注一致 |
| **证据关联覆盖率** | ≥90% | 87.5% ⚠️ | 计划通过 few-shot 增强在下一迭代补齐 |
| **冲突检测 Recall** | ≥85% | 80% ⚠️ | 计划通过规则扩展补齐 |

> ⚠️ **诚实声明**：证据关联覆盖率与冲突召回率尚未达标——我们没有包装成"全满分"，而是在 PRD / AUDIT 中主动声明缺口与改进计划。这是工程团队对真实性的承诺。

---

### 3. 具体方案说明（突出 AI 能力）

#### 3.1 总体架构：飞书内嵌入口 + 独立可信决策内核

```
┌─────────────────────────────────────────────────────────────────┐
│  飞书侧（feishu-orchestrator @ :8080）                          │
│  · 妙记/事件接入 + HMAC-SHA256 验签                             │
│  · 飞书 Aily MCP 接入（10 个工具 / 5 个 Webhook 事件）           │
│  · 飞书 Skill 提示词（10 步可信决策流程）                        │
│  · 飞书卡片 / 任务 / 多维表格 / 知识库适配器                    │
│  · 幂等（O_CREAT|O_EXCL 原子抢占）+ 重试（按 HTTP code 分类）    │
└──────────────────────┬──────────────────────────────────────────┘
                       │ 接口契约（4 条路由 + Bearer 鉴权）
                       │ ① POST /api/v1/meetings
                       │ ② POST /api/v1/candidates
                       │ ③ POST /api/v1/card/callback
                       │ ④ POST /api/v1/task/status
                       ▼
┌─────────────────────────────────────────────────────────────────┐
│  平台侧（labmemory-platform @ :8081）                           │
│  FastAPI + React 19 + Vite 6 + SQLAlchemy + SQLite/PostgreSQL  │
│  · 六道闸门 + 冲突检测（trust_rules）                          │
│  · 复核 → 主张（参数级版本）→ 任务                              │
│  · 行动前审计（五项检查三态）                                    │
│  · 结果回流 + 知识发布（5 态 + 失败边界卡 + 复验任务）            │
│  · 实验护照 + 可信问答（混合检索 RAG + 流式 SSE）               │
│  · 会前研讨包 + 控制塔 + 决策收件箱                              │
└─────────────────────────────────────────────────────────────────┘
                       ▲
                       │ Aily MCP 双向调用（机器对机器 + Bearer）
                       │
┌─────────────────────────────────────────────────────────────────┐
│  飞书 Aily 企业版（云端 LLM）                                    │
│  · Skill 提示词（10 步决策流程）                                │
│  · MCP Client（SSE 传输）                                      │
│  · 自动调 labmemory_* 10 个工具                                │
└─────────────────────────────────────────────────────────────────┘
```

#### 3.2 AI 与确定性规则的分工（项目最核心的设计哲学 + 代码层落地）

| 环节 | 谁负责 | 为什么 | 代码位置 |
|---|---|---|---|
| 从逐字稿抽取参数候选、主张、证据锚点、任务 | **飞书 Aily（LLM）** | 长文本语义理解是 AI 强项 | `feishu-orchestrator/adapters/aily_adapter.py`（`_real_compile` + `_build_prompt`） |
| 候选 JSON 解析失败兜底 | **确定性规则** | 防 LLM 输出格式异常导致链路卡死 | `_parse_skill_output` + `_extract_json_from_text`（解析失败显式 `ValueError`，不静默返回空候选） |
| 候选边界强制（防 AI 越权） | **确定性规则** | 防 Aily 直接产出 `status=approved` | `_rule_validate` 强制 `cand["status"] = "candidate"`、`needs_review = True` |
| experiment_ref 格式校验 | **确定性规则**（正则） | 防 AI 编造实验编号 | `_rule_validate` 中 `r'^[A-Za-z][A-Za-z0-9_\-]*$'` |
| 参数单位提取（温度/时间/百分比等） | **确定性规则** | AI 不会精确拆分"70℃ min"这种字符串 | `_extract_unit`（6 种正则模式） |
| 候选能否成为生效主张（六道闸门） | **确定性规则引擎** | "什么生效"不能由 AI 决定 | `app/services/trust_rules.py::run_six_gates` |
| 语义栅栏（"可以试试/建议/可能/暂定"不生效） | **确定性规则** | 防 AI 把试探当结论 | `trust_rules.py::SEMANTIC_HEDGE_KEYWORDS`（7 个关键词） |
| 数值冲突检测（同 scope 同参不同值） | **确定性规则** | 防数值打架 | `trust_rules.py::detect_conflicts`（`_value_key` 归一化 float） |
| 版本比对、supersede | **确定性规则** | 历史永久可追溯 | `_build_claim` 中"按 `created_at desc` 取首条最新 current 之外的 current 才参与冲突判定" |
| 多轮问答的指代消解改写 | **LLM 改写** | 历史原文不直接进入召回 | `qa.py` → `rag.py::ask_stream_events` → `llm.classify_intent` |
| 证据可达性探活 | **结构化校验 + 后续 URL 探活** | 区分"有证据"和"证据有效" | `evidence.py`（结构化校验：非空 + 含 `text` + 关联锚点） |
| 权限收敛（PI/Lead/Executor） | **确定性规则** | 防止越权 | `app/api/deps.py::require_pi/require_pi_or_lead/require_member/ensure_experiment_member` |
| 机器鉴权 | **确定性规则**（HMAC 常量时间比较） | 防时序攻击 | `app/api/deps.py::verify_platform_api_key`（`hmac.compare_digest`） |
| 登录限速 | **确定性规则** | 防爆破 | `auth.py`（同 (用户名,IP) 5 次失败锁 15 分钟） |
| 幂等（防重复事件） | **确定性规则**（原子文件锁） | 防 TOCTOU 竞态 | `reliability/idempotency.py::acquire`（`O_CREAT|O_EXCL`） |
| 重试（按 HTTP code 分类） | **确定性规则** | 防盲目重试 | `reliability/retry_engine.py::is_retryable`（9999999/502/503/504 才重试） |
| Webhook 验签 + 时间窗 | **确定性规则**（HMAC + 时间戳） | 防重放 + 防篡改 | `core/webhook_server.py::_verify_request`（5 分钟时间窗 + SHA256） |

#### 3.3 主链路走查（妙记 → 知识发布，10 步流程 + 状态机 + 接口契约）

```
┌─ 飞书侧（feishu-orchestrator @ :8080）───────────────────────────┐
│ 1. 飞书会议结束 → 妙记生成事件（v2.0 schema）                    │
│    → 编排器事件路由（O_CREAT|O_EXCL 原子幂等抢占）                │
│    → 状态：WAITING_MINUTES → MINUTES_READY                      │
│ 2. 组装 MeetingPackage（_normalize_meeting_package 归一化）      │
│    · meeting_id == source_object_id（防三处 ID 不一致）           │
│    · metadata.experiment_id 注入（缺失回退 EXP-DEMO-001）         │
│    → POST /api/v1/meetings 注册会议到平台                        │
│ 3. 飞书 Aily Skill 编译（_segment_meeting 长会议分段，≤8000字） │
│    → CandidatePackage（_rule_validate 确定性校验后）             │
│    · 强制 cand.status = "candidate"、needs_review = True        │
│    · experiment_ref 正则 ^[A-Za-z][A-Za-z0-9_\-]*$               │
│    → POST /api/v1/candidates 提交                              │
│ 4. 平台返回 {status: "submitted", review_count: N, results[]}    │
│    → 编排器状态：SUBMITTED                                      │
│ 5. 发飞书复核卡（≤3 张，fire-and-forget）                       │
│    → 编排器状态：REVIEWING                                      │
└──────────────────────────────┬──────────────────────────────────┘
                               ▼
┌─ 平台侧（labmemory-platform @ :8081）───────────────────────────┐
│ 6. 复核人在卡片/平台 confirm_review / card_callback(approve)     │
│    → 触发【六道闸门 run_six_gates + 冲突检测 detect_conflicts】│
│    · 状态：MeetingReview.status pending → processed              │
│    · 实验写锁 lock_experiment_for_write 防并发竞态              │
│    · 全过 → 生成 current 主张（按参数维度版本化、旧 superseded）│
│    · 证据/语义未过 → pending_validation；对象/参数/范围/责任    │
│      未过 → pending_supplement（不建任务）                     │
│    · 数值冲突（同 scope 同参不同值）→ 冻结发布                 │
│ 7. 行动前审计 _run_checks（五项检查）                            │
│    · version_unit：计划参数版本是否为 current                    │
│    · evidence_scope：证据/适用范围/失败边界是否齐全             │
│    · approval：审批门 status                                    │
│    · resource：资源门 status                                    │
│    · failure_boundary：失败边界确认 ack                          │
│    → passed → 任务 audited；needs_confirmation / blocked         │
│ 8. 任务启动 → 执行 → 结果提交（submitted，每任务一条 Result）    │
│    · Result.actual_params 与 planned_params 偏差冻结             │
│ 9. 知识发布（5 态）：partially_supported → 附失败边界卡 + 复验   │
│    · failure_boundary = {phenomenon, root_cause_status, next}    │
│    · model_feedback = {model_version, prediction, actual, ...}   │
│ 10. 实验护照：按会议组织数据链、按实验组织时间线                 │
│     · EmbeddingChunk 同步索引（claim→task→result→boundary→evidence）│
│     · RAG 检索可命中最新已发布结果                             │
└──────────────────────────────┬──────────────────────────────────┘
                               ▼
┌─ Webhook 推回飞书侧（HMAC-SHA256 签名 + 幂等键去重）─────────────┐
│ decision.pending → 复核卡（fire-and-forget）                    │
│ preflight.blocked → 阻断卡                                     │
│ execution.deviated → 偏差通知                                  │
│ knowledge.ready → 飞书知识库 + 复验提醒                         │
│ reverify.due → 复验任务卡                                      │
└─────────────────────────────────────────────────────────────────┘
```

**10 个核心状态机枚举**（`core/state_machine.py::MeetingState`）：
`WAITING_MINUTES → MINUTES_READY → COMPILING → SUBMITTED → REVIEWING → APPROVED → COMPLETED` / `BLOCKED` / `FAILED`

**接口契约**（`openspec/specs/contracts/spec.md`，版本化冻结）：

| 路由 | 鉴权 | 输入 | 输出 |
|---|---|---|---|
| `POST /api/v1/meetings` | Bearer `PLATFORM_API_KEY` | `MeetingPackage`（metadata.experiment_id 必填） | `{meeting_id, id, created}` |
| `POST /api/v1/candidates` | Bearer | `CandidatePackage`（source_package_id 必填引用已有 meeting） | `{id, created, status:"submitted", results[], review_count}` |
| `POST /api/v1/card/callback` | Bearer | 嵌套飞书信封 `{token, open_id, action:{value:{action_type,candidate_id}}}` | `approve→{status:approved/blocked, action_audit}` / `reject→{status:rejected}` / `revise→{status:blocked}` |
| `POST /api/v1/task/status` | Bearer | `{candidate_id, status:pending/success/failed, feishu_task_guid, error?}` | `{ok, task_id, feishu_task_guid}` |
| `GET /api/v1/candidates/{id}` | Bearer | — | 扁平候选 + `meeting_title` 富化 |

#### 3.4 两个核心 Demo 故事（评测样例已备齐）

**Demo 1 — 旧参数拦截（行动前审计阻断）**

```
C001 主张「80℃ v1」/ 会议 M001     →    C003 主张「70℃ v2」/ 会议 M002
   │                                       │（replaces C001）
   │                                       │
   │    新成员从旧纪要创建 80℃ 任务 ───────┼──→ 行动前审计
   │                                       │   ↓
   │                                       │   ❌ blocked
   │                                       │   原因：版本不匹配
   │                                       │   证据：70℃ 收率 78% vs 80℃ 收率 72%
   │                                       │   一键修正 ↓
   │                                       │   ✓ 一键改为 70℃ → 创建飞书任务
```

证据（评测样例）：复现实验 EXP-0707-04 在 70℃ 收率 **78%**，EXP-0707-05 在 80℃ 收率 **72%**；适用范围均为 S1、0.20 mol/L、2h、催化剂 B。

**Demo 2 — 结果回流（非二元知识状态）**

```
65℃（主张 C007）使转化率 63% → 78%   ✅   + 副产物 4% → 9%   ⚠️
   ↓
   系统将决策更新为「partially_supported」
   ↓
   自动生成失败边界卡
   · 现象：副产物超标
   · 根因假设：高温与催化剂当量共同促进副反应（标注"假设"）
   ↓
   自动创建复验任务：催化剂降到 0.8 eq 复验
```

#### 3.5 飞书 Aily MCP 接入详情（深度使用飞书 AI 能力）

##### 3.5.1 协议与传输

**协议**：MCP（Model Context Protocol）标准 JSON-RPC over SSE。
**传输**：HTTP + SSE，Aily 通过 `GET /mcp/sse` 建立 server→client 流，`POST /mcp/messages` 发送 client→server 消息。
**端点**：`https://<your-domain.com>/mcp/sse?token=<PLATFORM_API_KEY>`
**鉴权**：因 Aily 后台只支持 name + url + desc 三项（不支持 Authorization Header），采用 queryParam 鉴权（参考高德 MCP `?key=` 模式）。

**握手流程**：
```
Aily ─── GET /mcp/sse?token=xxx ──→ MCP Server
Aily ←── event: endpoint ────────── MCP Server（返回 session_id UUID v4）
Aily ─── POST /mcp/messages ─────→ MCP Server（initialize + notifications/initialized）
Aily ─── POST /mcp/messages ─────→ MCP Server（tools/list → 返回 10 个工具清单）
Aily ─── POST /mcp/messages ─────→ MCP Server（tools/call → 调用具体工具）
```

**鉴权设计**（三层）：
- 鉴权只在 SSE 握手（GET /sse）时校验一次；后续 POST /messages 由 MCP SDK 自动分发的 UUID v4 session_id 路由（128 位熵）。
- 优先级：`Authorization: Bearer` > URL `?token=` / `?key=` / `?api_key=`（三种命名兼容，高德用 key、Aily 文档示例用 token）。
- 备用方案：`MCP_REQUIRE_AUTH=false` 无鉴权模式 + nginx IP rate limit（`burst=40 nodelay`），适用于 Aily 后台 URL 校验拒绝带 `?token=` 的场景。

**身份模型**（机器对机器，非用户身份）：
- MCP Server **不接受** `x-aily-user` 之类客户端自报身份头（防伪造）。
- 用户身份由 **Aily 模型在工具参数里显式传入**（`reviewer` / `assignee` / `executor`），由平台侧 `_resolve_actor()` 按 username / feishu_user_id 解析。
- 解析失败时按既定规则降级（如默认回 PI），该回退写入 `AuditEvent` 留痕。
- 审计追溯走平台 `actor_id` + webhook `correlation_id`。

##### 3.5.2 10 个工具清单（按 10 步流程串起）

| # | 工具 | 一句话作用 | 关键参数 | 幂等性 |
|---|---|---|---|---|
| 1 | `labmemory_submit_transcript` | 收妙记/逐字稿 | `meeting_id`（必填）、`experiment_id`、`speakers[]`、`segments[{start,end,speaker,text}]`、`title`、`summary` | meeting_id 重复 → created:false |
| 2 | `labmemory_submit_extraction` | Aily 抽取结果回写 | `transcript_id`（必填）、`params[{name,value,unit,evidence[]}]`、`disputes[]`、`risks[]`、`task_candidates[]`、`scope{}`、`evidence[]`、`confidence` | extraction_id 重复 → idempotent |
| 3 | `labmemory_create_review` | 建决策复核项 | `extraction_id`、`assignee`、`jump_url` | decision_id 唯一 |
| 4 | `labmemory_submit_verdict` | 复核 verdict 通过/驳回 | `decision_id`、`verdict`（pass/reject）、`reviewer`、`comment` | verdict 不可逆 |
| 5 | `labmemory_issue_version` | 签发参数版本 | `decision_id`、`params[]`、`effective_from`、`evidence_refs[]` | **幂等**：已签发返回相同 version_id |
| 6 | `labmemory_preflight_check` | 执行前自检（六道闸门） | `version_id`、`executor`、`planned_at` | verdict: ok/block |
| 7 | `labmemory_submit_execution` | 实际执行结果回写 | `version_id`、`actual_params{}`、`results{}`、`executor`、`executed_at` | frozen 判定（版本偏差） |
| 8 | `labmemory_update_passport` | 更新护照/主张/边界 | `passport_id`、`status`、`failure_boundary{}`、`claim_state` | updated:true |
| 9 | `labmemory_publish_knowledge` | 发布知识 | `passport_id`、`title`、`summary`、`doc_refs[]` | **幂等**：同实验最新已发布再调返回原 knowledge_id + `"idempotent": true` |
| 10 | `labmemory_create_reverify` | 创建复验任务 | `passport_id`、`assignee`、`due_at`、`criteria[]` | reverify_id 唯一 |

##### 3.5.3 工具 Schema 示例（节选自 `tools-manifest.json`）

**`labmemory_submit_transcript`**（收妙记/逐字稿）：
```json
{
  "name": "labmemory_submit_transcript",
  "description": "收妙记/逐字稿：把飞书会议 transcript 写入平台。调用前必须已知 meeting_id 与 experiment_id。",
  "inputSchema": {
    "type": "object",
    "required": ["meeting_id"],
    "properties": {
      "meeting_id": {"type": "string", "description": "会议唯一 ID"},
      "experiment_id": {"type": "string", "description": "实验编号（缺失回退 EXP-DEMO-001）"},
      "title": {"type": "string"},
      "summary": {"type": "string"},
      "speakers": {"type": "array", "items": {"type": "string"}},
      "segments": {"type": "array", "items": {"type": "object", "properties": {
        "start": {"type": "number"}, "end": {"type": "number"},
        "speaker": {"type": "string"}, "text": {"type": "string"}
      }}}
    }
  }
}
```

**`labmemory_preflight_check`**（执行前自检）：
```json
{
  "name": "labmemory_preflight_check",
  "description": "执行前自检（六道闸门）",
  "inputSchema": {
    "type": "object",
    "required": ["version_id", "executor"],
    "properties": {
      "version_id": {"type": "string"},
      "executor": {"type": "string"},
      "planned_at": {"type": "string", "format": "date-time"}
    }
  }
}
// 返回：{"verdict": "ok" | "block", "reasons": [...], "boundary_check": {...}, "task_id": "T_xxx"}
```

##### 3.5.4 Aily Skill 提示词工作流（10 步，`docs/aily-skill/skill-prompt.md`）

**角色定位**（提示词核心约束）：
> "你是飞书 Aily，集成在飞书会议与妙记里，是 LabMemory 在飞书侧的唯一入口。你**不**直接执行实验、不直接发飞书任务、不直接改参数版本——所有可信决策都通过下面 10 个 MCP 工具完成；你在链路里是「搬运工 + 传话人」，判断与放行由 LabMemory 的闸门和人来完成。"

**10 步流程**（每场会议按序走一遍）：
1. **收逐字稿**：妙记就绪后立刻调 `submit_transcript`（记下 transcript_id + meeting_id）
2. **抽取参数**：从逐字稿识别参数候选/争议/风险/任务候选 → `submit_extraction`（evidence 必带 text、scope 必须对象）
3. **建复核项**：`create_review` 让平台推卡片给 PI/Lead
4. **复核 verdict**：人在平台完成（Aily 不代替人，除非被指定为复核人）
5. **签发参数版本**：`issue_version`（verdict=pass 后兜底，幂等）
6. **执行前自检**：`preflight_check`（六道闸门）
7. **执行回写**：`submit_execution`（actual_params + results）
8. **护照更新**：`update_passport`（主张状态 + 失败边界）
9. **知识发布**：`publish_knowledge`（幂等）
10. **创建复验**：`create_reverify`（失败边界驱动的复验任务）

##### 3.5.5 5 个 Webhook 事件（平台主动推回飞书侧）

| 事件 | 触发时机 | 推送内容 | 幂等键 |
|---|---|---|---|
| `decision.pending` | 决策项创建 | 飞书卡片给 PI/Lead | `decision.pending:DEC-xxx` |
| `preflight.blocked` | 自检不通过 | 飞书任务卡片给执行人 | `preflight.blocked:T-xxx` |
| `execution.deviated` | 偏差超阈 | 偏差通知给 PI | `execution.deviated:R-xxx` |
| `knowledge.ready` | 知识可发布 | 飞书知识库 + 复验提醒 | `knowledge.ready:R-xxx` |
| `reverify.due` | 复验到期 | 飞书任务卡片给 assignee | `reverify.due:RV-xxx` |

**签名与重试**：
- `X-LabMemory-Signature` = HMAC-SHA256(secret, timestamp + nonce + raw_body)，hex 编码
- `X-LabMemory-Timestamp`（秒级时间戳）+ `X-LabMemory-Nonce`（随机串）+ `X-LabMemory-Idempotency-Key`（幂等键）
- 5xx 网络错误：平台最多重试 5 次（指数退避），幂等键不变
- 飞书侧 200 OK 但 body 含 `{"ok": false}`：平台按 5xx 处理继续重试
- 飞书侧返回 `{"ok": true, "duplicate": true}`：平台记录后停止重试

**返回值精简**（防 token 超限）：所有工具返回 ≤ 2 万字（去掉 transcript 全文等大字段）；完整数据通过 `jump_url` 在飞书卡片里给用户跳转。

**DB Session 隔离**：每个工具调用独立 SessionLocal，事务内完成；`CancelledError` 时显式 rollback + close（避免 SQLite 单连接池被污染）。

#### 3.6 可信决策内核（对抗审查 Tier 2 已落地为代码 + 规则逐条）

**核心代码位置**：`labmemory-platform/app/services/trust_rules.py`（可信规则引擎）+ `app/api/tasks.py::_run_checks`（行动审计）+ `app/db/locking.py::lock_experiment_for_write`（写锁）。

##### 3.6.1 六道质量闸门（候选→主张发布前）

```python
# 7 个语义栅栏关键词（PRD line 854）
SEMANTIC_HEDGE_KEYWORDS = ["可以试试", "建议", "可能", "暂定", "试试", "或许", "考虑"]

def run_six_gates(candidate, modifications, experiment, reviewer) -> dict:
    """六道闸门（PRD §10.1）。返回 {gates, failed, publish_status, passed}。

    发布状态判定：
      · hard_gates = {object, parameter, scope, responsibility} 任一失败 → pending_supplement（不建任务）
      · soft_gates = {evidence, status} 任一失败 → pending_validation
      · 全过 → current（建主张 + 任务草稿）
    """
```

| 闸门 | 判定逻辑 | 失败后果 | 类别 |
|---|---|---|---|
| **① 对象门** | `experiment_ref` 或 `experiment.experiment_id` 非空 | pending_supplement | hard |
| **② 参数门** | `len(parameters) > 0` 且每个参数都 `name + value` | pending_supplement | hard |
| **③ 证据门** | `len(evidence) > 0` 且每个证据都含 `text` | pending_validation | soft |
| **④ 范围门** | `scope` 非空（材料/浓度/设备/批次等边界） | pending_supplement | hard |
| **⑤ 状态门**（语义栅栏） | 候选原文 + 复核修改值合并文本，7 个 hedge 关键词全部不命中 | pending_validation | soft |
| **⑥ 责任门** | `reviewer is not None`（操作者已识别） | pending_supplement | hard |

**关键设计点**：
- 语义栅栏扫描范围 = 候选原文 + `modifications.title` + `modifications.description` + 所有参数 value 的合并文本——**仅扫候选原文可被复核修改绕过**，必须合并扫。
- hard/soft 分类决定了发布状态——soft 失败仍可建主张（`pending_validation`），hard 失败不建任务。

##### 3.6.2 参数级版本模型（按参数维度跟踪版本，未变参数继承不升版）

```python
# Claim.parameter_version JSON 格式：
{
  "scope": {"material": "Compound-A", "concentration": "0.20mol/L", ...},
  "parameters": [
    {"name": "temperature", "value": "70", "unit": "℃", "version": 2},
    {"name": "rpm", "value": "300", "unit": "rpm", "version": 1},
  ]
}
```

- **替代关系**：新 Claim 通过 `replaces_claim_id` 指向旧 Claim，旧 Claim 状态置 `superseded`。
- **按参数维度升版**：只对 `modifications.parameters` 中**实际改变的参数**升版本，未变参数继承旧版本号。
- **范围门守门**：仅在 `scope` 等价（`_scope_eq` 字典 None 值容忍）的前提下才视为同主张，**不同适用范围的 current 主张可以并存**（见 Round 3 修复 `2026-08-14-fix-trust-core-round3`）。

##### 3.6.3 6 类冲突检测（PRD §10.5）

```python
def detect_conflicts(db, experiment_id, new_params, new_scope, reviewer) -> list[dict]:
    """返回 [{type, severity, message, existing_claim}, ...]"""
```

| 冲突类型 | 判定逻辑 | severity |
|---|---|---|
| **数值冲突** | 同 scope 同参数名 + 当前 current（非本次 supersede 目标）+ 不同值 | **high** → 冻结发布 |
| **版本冲突** | 任务引用旧参数版本（复用 _run_checks.version_unit） | high |
| **范围冲突** | 同参数名但 scope 不等价（材料/浓度/设备差异） | medium → 人工消歧 |
| **语义冲突** | 关键词反义（如"升温"vs"降温"） | medium |
| **证据冲突** | 同锚点不同结论（复用 evidence 闸门） | medium |
| **责任冲突** | `reviewer is None` | high（与责任门互补） |

**数值冲突数值归一化**：
```python
def _value_key(v) -> float | str:
    """统一转 float（70 == 70.0 == "70"），非数值退 str。"""
    try:
        return float(v)
    except (TypeError, ValueError):
        return str(v)
```

##### 3.6.4 行动前审计（任务执行前五项检查）

`app/api/tasks.py::_run_checks` 返回 `{overall, items, reasons, confirmations}`，五项检查：

| 检查项 | 判定逻辑 | 失败 → 任务状态 |
|---|---|---|
| **version_unit** | 计划参数与 current 主张的参数版本比对 | blocked（一键修正） |
| **evidence_scope** | 主张证据 + 适用范围 + 失败边界齐全 | needs_confirmation |
| **approval** | `Task.approval_status == "approved"` | needs_confirmation |
| **resource** | `Task.resource_status == "ready"` + 物料/设备/排期非空 | needs_confirmation |
| **failure_boundary** | `Task.failure_boundary_ack == True`（用户主动 ack） | needs_confirmation |

**三态语义**：
- `passed`：全过 → 任务可启动
- `needs_confirmation`：补审批/资源/知悉失败边界后重审
- `blocked`：版本错配 → 一键修正为当前版本

##### 3.6.5 知识 5 态 + 失败边界卡

```python
# Result.knowledge_status 5 态：
"supported"            # 完全支持
"partially_supported"  # 部分支持（带失败边界卡 + 复验任务）
"refuted"              # 推翻
"replaced"             # 被新主张替代
"insufficient_evidence"# 证据不足
```

**失败边界卡结构**：
```json
{
  "phenomenon": "副产物从 4% 升至 9%（超标）",
  "trigger_condition": "65℃ 高温 + 催化剂当量 ≥ 1.0 eq",
  "ruled_out": "排除温度漂移、设备误差",
  "root_cause_status": "假设（高温 + 催化剂当量共同促进副反应）",
  "next_step": "复验：催化剂降到 0.8 eq，温度 65℃"
}
```

**根因假设必须显式标注"假设"**——这是科学严谨性的底线，不能让 AI 自动生成的"根因"被无证据地采信。

#### 3.7 可信问答（混合检索 RAG + 流式 SSE + 9 步拒答路径）

**核心代码**：`app/services/rag.py`（混合检索引擎）+ `app/api/qa.py`（端点 + 会话管理）+ `app/services/llm.py`（DeepSeek 对话）+ `app/db/vec.py`（sqlite-vec 向量）+ `app/services/indexer.py`（索引同步）。

##### 3.7.1 9 步检索流程（`rag.py::ask_stream_events`）

```
1. 会话解析（_resolve_session）── 缺省新建，传入则校验 owner + 归档状态
2. 意图识别（llm.classify_intent）── LLM 判定 rag/chat（chat 跳过检索阶段）
3. 加载历史（_load_history）── 摘要 + 最近 N 轮上下文窗口
4. chat 路径：跳过检索 → _answer_chat 直接生成
5. rag 路径：检索 → 步骤 6/7/8/9
6. 权限前置过滤（_user_experiments）── admin/pi 全局可见，executor 仅成员实验
7. 状态过滤（_candidate_chunk_ids）── claim.current + knowledge_status ∈ {null, supported, partially_supported}
8. BM25 召回（vec.fts_search）── chunks_fts 虚拟表
9. 向量召回（vec.vec_search）── vec_chunks 虚拟表（sqlite-vec）
10. 关系扩展（_relation_expand）── claim→task→result→boundary→evidence
11. 重排（_rerank）── 5 维加权融合
12. 上下文组装 + 引用后处理 + 待验证提示
13. 回答 agent 生成（llm.chat_with_history）── 带历史与摘要
14. 拒答检测（detect_refusal）── 显式 REFUSED: <原因>
15. 会话持久化 + SSE emit done
```

##### 3.7.2 混合检索 + 重排的 5 维评分公式

```python
# 重排（rag.py::_rerank）
total = (
    BM25_WEIGHT * bm25_norm          # 默认 0.4
  + VECTOR_WEIGHT * vec_norm         # 默认 0.4
  + RECENCY_WEIGHT * recency         # 默认 0.1（30 天半衰期）
  + STATUS_WEIGHT * status_w         # 默认 0.1
) * type_w                            # 类型偏置：claim(1.0) > result(0.85) > boundary(0.7) > evidence(0.6)
```

| 评分维度 | 含义 | 权重（可配） |
|---|---|---|
| `bm25_norm` | BM25 相似度（越小越相关）→ 归一化 [0,1] | 0.4 |
| `vec_norm` | 向量距离 → 转换为相似度 `1/(1+dist)` | 0.4 |
| `recency` | 时间新鲜度（30 天半衰期 `exp(-days/30)`） | 0.1 |
| `status_w` | 知识状态权重：`supported=1.0` / `partially_supported=0.7` / `None=0.5` / `refuted=0.2` | 0.1 |
| `type_w` | chunk_type 偏置：claim 优先于 result 优先于 boundary | × 系数 |

**关键设计点**：
- `STATUS_WEIGHT` 让 `refuted` 自动降权 0.2（被推翻的知识不优先召回）。
- `recency` 半衰期 30 天让"老经验"自然降权，新结果优先。
- `type_w` 让 claim（主张）优先于 evidence（证据）——主张是结论，证据是支撑。

##### 3.7.3 关系扩展（`rag.py::_relation_expand`）

```
命中的 claim chunk
   │
   ├─→ 拉关联 Task（Task.claim_id = claim.id）
   │      │
   │      └─→ 拉 published Result（Result.task_id IN tasks）
   │             │
   │             └─→ 拉 failure_boundary（Result.failure_boundary 非空）
   │
   └─→ 拉关联证据（Evidence.ref_id LIKE "{meeting_id}#%"）
```

**为什么需要关系扩展**：纯 BM25/向量召回只能召回"用户问的关键词所在 chunk"，但会议讨论里的"参数变更 + 结果反馈 + 失败边界"通常是**多 chunk 跨对象**的——关系扩展把这些跨对象的相关 chunk 全部拉回来。

##### 3.7.4 拒答路径（4 种，缺一不可）

```python
# rag.py::ask_stream_events 中 4 个 yield refused 的位置：
1. no_permission ───────────── 用户无可见实验
2. no_match_after_status_filter ── 状态过滤后无候选
3. score_below_threshold ──── 最高相似度 < RAG_MIN_SCORE
4. llm_refused ────────────── LLM 输出含 "REFUSED:" 前缀
```

**关键设计点**：
- 每种拒答都带 `missing_conditions`（告诉用户"还缺什么"） + `retrieval_details`（告诉用户"为什么拒"）。
- `refused=true` 显式返回，前端不显示"答案"区，改为"可信证据不足"提示。

##### 3.7.5 流式 SSE 端点（`/api/qa/ask/stream`）

**事件序列**：
```
session → intent → (rag: retrieval_started → retrieval_completed | refused)
       → answer_started → answer (| refused) → done
```

**前端切换文案**（基于事件名）：
- `session`：显示 session_id
- `intent`：显示"正在理解您的问题..."
- `retrieval_started`：显示"正在检索可信证据..."
- `retrieval_completed`：显示"已找到 N 条相关证据"
- `answer_started`：切换为打字机效果
- `answer`：流式追加 answer 文本
- `refused`：切换为"证据不足"提示
- `done`：结束

**实现要点**（qa.py）：
- 与同步端点 `ask` 共用 `rag.ask_stream_events` 生成器，逻辑零重复。
- `StreamingResponse` + `X-Accel-Buffering: no` 防 nginx 缓冲。
- `Cache-Control: no-cache`。

##### 3.7.6 降级路径（防单点故障）

- **向量检索不可用**：自动降级为 BM25 + 关系扩展 + 重排（不再返回 503，`retrieval_details.vector_available=false`）。
- **LLM 不可用**：降级为模板式回答（`chat_mode="template-fallback"`）。
- **Embedding 不可用**：降级为 hash-fallback（`embedding_mode="hash-fallback"`）。
- **索引为空**：首次问时自动 `ensure_index_ready` 全量重建。

##### 3.7.7 多轮会话与指代消解

- **会话持久化**：每次问答保存 `QAMessage`（role + content + citations + refused + retrieval_details + intent）。
- **上下文窗口**：`[system] + [history_summary system] + 最近 QA_HISTORY_TURNS 轮历史 + 本轮 user`
- **指代消解**：用户问"上次说的 80℃"→ LLM 改写为自包含"80℃ v1 主张"→ 召回时按改写查询，历史原文不直接进入召回。
- **滚动摘要**：超出窗口的旧消息折叠为 300 字摘要（`summary_cursor` 幂等游标，单调递增）。

#### 3.8 可靠性与鲁棒性（不是 PPT，是代码 + 每一项都有测试覆盖）

##### 3.8.1 幂等原子化（`reliability/idempotency.py`）

```python
class IdempotencyGuard:
    def acquire(self, idempotency_key: str) -> bool:
        """原子抢占处理权（O_CREAT|O_EXCL）。成功=True（获得执行权）；False=已被占/已处理。
        消除 check→业务→mark 三步 TOCTOU：调用方在执行业务副作用前 acquire，
        失败说明另一并发/重试已占，直接返回缓存或「处理中」。
        """
        import os
        try:
            fd = os.open(str(key_path), os.O_CREAT | os.O_EXCL | os.O_WRONLY)
            os.close(fd)
            return True
        except (FileExistsError, OSError):
            return False
```

**三种状态**：
- `acquire()`：原子抢占（O_CREAT|O_EXCL）——**消除 TOCTOU 竞态**，并发重试只执行一次。
- `mark()`：标记已处理（写入结果 + TTL，默认 7 天）。
- `release()`：释放占位（仅在异常路径）——允许失败操作后续重试。

**实际应用位置**：
- `core/pipeline_orchestrator.py::run_from_minutes_url`（妙记处理）——幂等键 `pipeline:{source_id}`。
- `core/card_handler.py::handle_callback`（卡片回调）——幂等键 `card_callback:{callback_token}`。
- `core/event_router.py`（事件路由）——幂等键 `event:{event_id}`。

##### 3.8.2 重试引擎（`reliability/retry_engine.py`）

```python
class RetryEngine:
    def is_retryable(self, exception: Exception) -> bool:
        # 明确标记为不可重试的（如 NonRetryableError）→ False
        # 飞书限流错误（9999999 / rate limit）→ True
        # 网络相关（timeout / 502 / 503 / 504）→ True
        # 其他 → False（避免盲目重试 4xx 业务错误）
        ...

    def calculate_delay(self, attempt: int) -> float:
        delay = base_delay * (backoff_factor ** attempt)
        delay = min(delay, max_delay)
        if self.jitter:
            delay *= (0.5 + random.random() * 0.5)  # 防惊群效应
        return delay
```

**关键设计点**：
- 指数退避（base 1s × factor 2）+ 50%~100% 随机抖动（防惊群）。
- 按异常类型分类：只重试网络/限流，不重试 4xx 业务错误。
- 通过 `@with_retry(interface_name="aily.compile_segment")` 装饰器集成。

##### 3.8.3 状态机原子写（`core/state_machine.py::set_state`）

```python
def set_state(self, object_id, state, extra=None):
    """设置对象状态。"""
    tmp_path = path.with_name(f"{path.name}.tmp{os.getpid()}")
    with open(tmp_path, "w", encoding="utf-8") as f:
        json.dump(current, f, ensure_ascii=False, indent=2)
    os.replace(tmp_path, path)  # 原子替换
```

**关键设计点**：临时文件 + `os.replace` 原子替换——避免写一半崩溃留下截断 JSON。

##### 3.8.4 Webhook 验签（`core/webhook_server.py`）

```python
SIGNATURE_MAX_AGE_SECONDS = 300  # 5 分钟时间窗，超出视为重放
MAX_BODY_BYTES = 1024 * 1024     # 1MB 请求体上限，防内存打爆

# 验签流程：
# 1. 读 body（content_length > MAX_BODY_BYTES → 413）
# 2. 飞书加密推送解密（AES；缺失依赖显式报错，不静默）
# 3. URL 验证挑战（type=url_verification 时回显 challenge）
# 4. HMAC-SHA256 验签（mock 模式跳过；real 模式失败即 401）
# 5. 时间戳校验（超出 5 分钟视为重放，拒绝）
# 6. 路由到 event_router
```

**7 类验签测试**（编排器集成测试覆盖）：
- 正常签名（正）/ 篡改签名（反）/ 空密钥 / 过期时间戳 / 畸形 body / 加密推送 / URL 验证挑战。

##### 3.8.5 鉴权与限速

```python
# 平台鉴权（app/api/deps.py）
def verify_platform_api_key(authorization, x_platform_api_key):
    """机器鉴权：双接受 Authorization Bearer + X-Platform-Api-Key，
    HMAC 常量时间比较（防时序攻击）。"""
    bearer = authorization.split(" ", 1)[1].strip() if authorization
    expected = settings.PLATFORM_API_KEY
    if expected and (
        (bearer and compare_digest(bearer, expected)) or
        (x_platform_api_key and compare_digest(x_platform_api_key, expected))
    ):
        return True
    raise PermissionDeniedError("无效的 PLATFORM_API_KEY")

# 登录限速（auth.py）
# 同 (用户名, IP) 连续 5 次失败 → 锁定 15 分钟 → 429
```

**关键设计点**：
- HMAC 常量时间比较（`hmac.compare_digest`）——防时序攻击。
- 双接受 Bearer Header + X-Platform-Api-Key——兼容两种客户端。
- 登录限速——防爆破。

##### 3.8.6 不产生虚假成功

```python
# pipeline_orchestrator.py 失败处理：
except Exception as e:
    if idempotency_key:
        idempotency_guard.release(idempotency_key)  # 释放占位
    if reviewer_id:
        try:
            im_card_adapter.send_alert_card(...)  # 发告警卡
        except Exception:
            pass
    state_machine.set_state(..., MeetingState.FAILED, ...)
    raise  # 抛出，不静默吞掉
```

**关键设计点**：
- 失败 → 发告警卡（best-effort，不阻断抛出）。
- 失败 → 释放幂等占位（允许后续重试）。
- pipeline 返回 `status: "submitted"`（非 `"success"`）——诚实地告诉调用方"已提交但未确认"。

##### 3.8.7 卡片回调不静默 success（`core/card_handler.py`）

```python
# card_handler.handle_callback
if status == "approved" and action_audit == "pass":
    self._handle_approved(...)  # 建任务
elif status == "rejected":
    self._handle_rejected(...)  # 驳回
elif status == "blocked":
    self._handle_blocked(...)   # 阻断
else:
    # needs_confirmation / revise / 未知：不建任务，记阻断态，不静默 success
    state_machine.set_state(..., MeetingState.BLOCKED, ...)
```

**关键设计点**：6 种回调结果显式分流，无静默 success 路径。

##### 3.8.8 测试覆盖（全部可复现）

| 闸门 | 结果 | 复现命令 | 工作目录 |
|---|---|---|---|
| 平台 17 步 e2e 决策全链路 | ✅ 全过 | `python -m scripts.e2e_test` | labmemory-platform |
| 平台 pytest 单测 | ✅ 通过 | `pytest tests/ -q` | labmemory-platform |
| 真跨侧联动 cross_side_check | ✅ 5/5 | `python -m scripts.cross_side_check` | labmemory-platform |
| 反向联动 reverse_linkage_check | ✅ 7/7 | `python -m scripts.reverse_linkage_check` | labmemory-platform |
| 编排器集成测试 | ✅ 38/38（7 大类：事件路由/幂等/Aily 编译/平台集成/卡片/可靠性/验签） | `python tests/test_integration.py` | feishu-orchestrator |
| 编排器模块体检 | ✅ 11/11 | `python scripts/health_check.py` | feishu-orchestrator |
| OpenSpec 规格校验 | ✅ 17/17，活跃 change 归零 | `openspec validate --all` | 仓库根 |
| Aily MCP 工具清单自检 | ✅ 10 个工具就绪 | `GET /mcp/manifest` | 线上 |

##### 3.8.9 生产可用性：进程监督、自动恢复与每日备份（赛前加固，2026-08-16 公网实测）

不是"部署完就不管"——平台进程与数据都有制度化的可用性保障，全部有实测证据：

| 保障项 | 实现 | 实测证据 |
|---|---|---|
| 崩溃自动恢复 | systemd 托管（`labmemory-platform.service`，`Restart=always` + `RestartSec=3`），替代一次性启动脚本 | 对线上进程 `kill -9` 演练：**5 秒内自动拉起，`/health` 恢复 200** |
| 日志治理 | 日志收归 journald（自动轮转），替代无限增长的裸 log 文件 | `journalctl -u labmemory-platform -f` 实时可查 |
| 数据备份 | cron 每日 03:30 SQLite **在线备份**（`sqlite3 backup` API，不 cp 活库）+ `integrity_check` 校验 + 保留 7 份滚动 | 手工触发验证：4.6MB 备份落盘，完整性 ok |
| 安全响应头 | nginx 全路径（含静态资源）`X-Content-Type-Options: nosniff` + `X-Frame-Options: SAMEORIGIN` + `CSP frame-ancestors 'self'`，防 MIME 嗅探与点击劫持 | 公网 `curl -I` 对 `/` 与 `/assets/*.js` 均已携带 |
| 入口限额 | nginx `client_max_body_size 5m`（JSON API 够用即可）+ 每 IP 10r/s 限流 | 超限返回 413/429 |

> 这一节本身也走了 OpenSpec 流程：change `harden-deployment-and-perf`（proposal + spec 增量 +3/~1 条 + tasks），部署脚本固化在仓库 `deploy/` 目录，换服务器可复用。

#### 3.9 规范驱动的开发流程

全项目强制 **OpenSpec 规格驱动开发**（`CLAUDE.md` 第 0 节红线）：

- 任何系统行为变更必须先建 change 提案（proposal + spec 增量 + tasks）。
- 实现 → validate → archive（增量规格合入源真相）。
- 已沉淀 **17 个领域规格**、归档 **30+ 个 change**。
- 完成**三轮对抗性审查 + 第一性原理审查**，累计发现并修复 **~40+ 条缺陷**（安全越权、幂等竞态、迁移错位、虚假成功、契约不对齐……）。
- 形成 `AUDIT.md` 中"卖点实现度"对照表——**每个宣称的能力都能指到具体代码行**。

---

#### 3.10 数据库 Schema 详解（17 张核心表 + 2 个虚拟表）

`labmemory-platform/app/db/models.py` 定义了 17 张核心表，覆盖"用户→项目→实验→会议→候选→复核→主张→任务→审计→结果→护照→问答→集成映射"完整链路。

##### 3.10.1 实体关系图（精简版）

```
┌─────┐  ┌────────┐  ┌────────────┐  ┌───────────────────┐
│User │→│Project │→│Experiment  │→│ExperimentMember   │ (M:N, role: pi/lead/executor)
└──┬──┘  └────────┘  └──────┬─────┘  └───────────────────┘
   │                          │
   │                          ▼
   │                    ┌──────────┐  ┌─────────────────┐
   │                    │ Meeting │→│Candidate       │ (1:1 meeting_id+source_package_id)
   │                    └────┬─────┘  └─────────────────┘
   │                         │
   │                         ▼
   │                   ┌──────────────┐  ┌───────────┐
   │                   │MeetingReview │  │ Claim     │ → replaces_claim_id (self FK)
   │                   └──────────────┘  └─────┬─────┘
   │                                            │ claim_id
   │                                            ▼
   │                                       ┌────────┐
   │                                       │ Task   │ → feishu_task_guid (回写)
   │                                       └────┬───┘
   │                                            │ task_id
   │                                            ▼
   │                              ┌──────────┐  ┌────────────┐
   │                              │ActionAudit│ │ Result    │ → knowledge_status (5 态)
   │                              └──────────┘  └─────┬─────┘
   │                                                │ failure_boundary (JSON)
   │                                                ▼
   │                                          ┌──────────┐
   │                                          │Embedding │ (claim/result/evidence/boundary)
   │                                          │  Chunk   │
   │                                          └──────────┘
   │
   └→ ┌──────────┐  ┌──────────┐
      │QASession │→│QAMessage │ (会话壳 + 消息列表，按 user_id 隔离)
      └──────────┘  └──────────┘
      ┌──────────────┐
      │IntegrationRef│ (Aily ID ↔ 平台实体映射，避免领域表加外键)
      └──────────────┘
```

##### 3.10.2 17 张核心表清单

| # | 表 | 关键字段 | 索引 | 唯一约束 |
|---|---|---|---|---|
| 1 | `users` | `username`, `password_hash`, `global_role` (admin/pi/lead/executor) | `username` | — |
| 2 | `projects` | `project_id`, `pi_user_id` | `project_id` | — |
| 3 | `experiments` | `experiment_id`, `project_id`, `parameters_template` (JSON) | `experiment_id` | — |
| 4 | `experiment_members` | `experiment_id`, `user_id`, `role` | 两列均索引 | `(experiment_id, user_id)` |
| 5 | `meetings` | `meeting_id`, `experiment_id`, `transcript` (JSON), `raw_payload` | `meeting_id` | — |
| 6 | `candidates` | `meeting_id`, `source_package_id`, `candidates` (JSON), `risks`, `action_items` | `meeting_id`, `source_package_id` | `(meeting_id, source_package_id)` |
| 7 | `meeting_reviews` | `meeting_id`, `status` (pending/processed), `decision` (confirmed/ended), `modifications` (JSON), `notes`, `reason` | `meeting_id` | `(meeting_id,)` |
| 8 | `claims` | `claim_id`, `meeting_id`, `experiment_id`, `parameter_version` (JSON), `status` (current/superseded), `knowledge_status` (5 态), `replaces_claim_id` (self FK) | `claim_id` | — |
| 9 | `tasks` | `task_id`, `meeting_id`, `experiment_id`, `claim_id`, `status` (draft/audited/running/completed/blocked/needs_confirmation), `planned_params`, `approval_status`, `resource_status`, `failure_boundary_ack`, `feishu_task_guid` | `task_id`, `feishu_task_guid` | — |
| 10 | `action_audits` | `task_id`, `status` (pending/passed/blocked), `checks` (JSON 5 项), `result` (JSON) | `task_id` | `(task_id,)` |
| 11 | `results` | `result_id`, `task_id`, `actual_params`, `metrics`, `status` (submitted/published/frozen), `knowledge_status` (5 态), `failure_boundary` (JSON), `model_feedback` (JSON) | `result_id` | — |
| 12 | `audit_events` | `actor_id`, `action`, `target_type`, `target_id`, `before/after` (JSON), `reason`, `request_id` | `action`, `target_type`, `target_id`, `request_id` | — |
| 13 | `embedding_chunks` | `chunk_id`, `chunk_type` (claim/result/evidence/failure_boundary), `ref_id`, `experiment_id`, `status`, `knowledge_status`, `content_text`, `content_hash`, `metadata_json` | `chunk_id`, `chunk_type`, `ref_id`, `experiment_id`, `status`, `knowledge_status`, `content_hash` | — |
| 14 | `qa_sessions` | `user_id`, `title`, `archived`, `last_message_at`, `summary`, `summary_cursor` | `user_id`, `last_message_at`, `summary_cursor`, `archived` | — |
| 15 | `qa_messages` | `session_id` (CASCADE), `role` (user/assistant), `content`, `citations_json`, `refused`, `retrieval_details_json`, `model_info_json`, `missing_conditions_json`, `intent` | `session_id` | — |
| 16 | `integration_refs` | `ref_type` (transcript/extraction/decision/version/execution/passport/knowledge/reverify), `aily_id`, `platform_type`, `platform_id`, `payload` (JSON) | `ref_type`, `aily_id`, `platform_id` | `(ref_type, aily_id)` |
| 17 | `meetings` 等的 `IDMixin`/`TimestampMixin` | `id` (PK auto), `created_at`, `updated_at` | — | — |

**2 个虚拟表**（SQLite 扩展）：
- `vec_chunks` (sqlite-vec `vec0`)：向量存储，列 `(chunk_id TEXT, embedding FLOAT[dim])`，主键 chunk_id。
- `chunks_fts` (SQLite 内置 `FTS5`)：BM25 倒排索引，列 `(chunk_id UNINDEXED, content_text)`，tokenizer `unicode61`。

##### 3.10.3 关键索引设计

- **唯一约束防重复**：`meeting_reviews` 一会议一条；`action_audits` 一任务一条；`candidates` (meeting_id, source_package_id) 联合唯一；`experiment_members` (experiment_id, user_id) 联合唯一。
- **复合索引优化**：所有外键列（`experiment_id`/`meeting_id`/`task_id`/`session_id`/`claim_id`）均加单列索引；`(experiment_id, status)` 用于 RAG 状态过滤。
- **`content_hash` 索引**：EmbeddingChunk 用于索引去重（同一内容不重复入索引）。

##### 3.10.4 JSON 列的语义

| 字段 | 类型 | 写入方 | 读取方 |
|---|---|---|---|
| `Meeting.transcript` | list[dict] | 编排器 POST /api/v1/meetings | RAG 召回 / 实验护照 / 复核界面 |
| `Meeting.raw_payload` | dict | 编排器 POST（完整 MeetingPackage 备份） | 三值留痕溯源 |
| `Candidate.candidates` | list[dict] | 编排器 POST /api/v1/candidates | 六道闸门 pick_candidate |
| `MeetingReview.modifications` | dict | 复核确认接口 | 语义栅栏扫描 + 参数覆盖 |
| `Claim.parameter_version` | dict | _build_claim | _run_checks.version_unit / 护照时间线 |
| `Claim.content` | dict | _build_claim | 实验护照 / RAG content_text 组装 |
| `Task.planned_params` | dict | _build_task | _run_checks / 偏差冻结判定 |
| `Task.resources` | dict | /tasks/{id}/resources | 审计 JSON 展示 |
| `ActionAudit.checks` | dict | _run_checks | 审计结果展示 |
| `Result.actual_params/metrics` | dict | /tasks/{id}/submit-result | RAG / 护照 / 知识发布 |
| `Result.failure_boundary` | dict | 知识发布（partially_supported） | 失败边界卡展示 / RAG 召回 |
| `EmbeddingChunk.content_text/metadata_json` | str/dict | indexer.upsert_chunk | RAG 召回 / 引用后处理 |
| `AuditEvent.before/after` | dict | 各操作点 | 审计追溯 |

**参数不硬编码设计**：`Candidate.parameters` / `Claim.parameter_version` / `Task.planned_params` / `Result.actual_params` 全部 JSON，**完全由会议传入数据动态填充**——不做 schema 限制（不同于固定字段表），支持任意参数组合。

---

#### 3.11 API 端点完整清单（30+ 端点）

平台共 **30+ 个 REST 端点**，按权限分层：**机器鉴权（PLATFORM_API_KEY）** / **用户鉴权（JWT）** / **公开端点**。

##### 3.11.1 机器鉴权端点（`/api/v1/*`，PLATFORM_API_KEY）

| 端点 | 方法 | 鉴权 | 用途 |
|---|---|---|---|
| `/api/v1/meetings` | POST | Bearer | 编排器注册会议（妙记接收） |
| `/api/v1/candidates` | POST | Bearer | 编排器提交候选（Aily 编译结果） |
| `/api/v1/candidates/{id}` | GET | Bearer | 编排器获取候选详情（建任务摘要） |
| `/api/v1/card/callback` | POST | Bearer | 编排器转发飞书卡片回调（嵌套信封） |
| `/api/v1/task/status` | POST | Bearer | 编排器回写飞书任务状态 |

##### 3.11.2 用户鉴权端点（`/api/*`，JWT）

**认证**：
| 端点 | 方法 | 鉴权 | 用途 |
|---|---|---|---|
| `/api/auth/login` | POST | 公开 | 账号密码登录（限速 5 次/15 分钟） |
| `/api/auth/me` | GET | JWT | 当前用户信息 |

**会议与复核**：
| 端点 | 方法 | 权限 | 用途 |
|---|---|---|---|
| `/api/meetings` | GET | JWT | 复核会议列表（按可见实验过滤） |
| `/api/meetings/{id}` | GET | JWT+成员 | 会议详情 |
| `/api/meetings/{id}/chain` | GET | JWT+成员 | 会议证据链 |
| `/api/meetings/{id}/review` | POST | JWT+成员 | 复核确认/驳回（六道闸门触发） |

**任务与审计**：
| 端点 | 方法 | 权限 | 用途 |
|---|---|---|---|
| `/api/tasks/{id}` | GET | JWT+成员 | 任务详情 |
| `/api/tasks/{id}/audit` | POST | JWT+成员 | 行动前审计（五项检查三态） |
| `/api/tasks/{id}/approve` | POST | JWT+PI/Lead | 审批通过 |
| `/api/tasks/{id}/reject` | POST | JWT+PI/Lead | 审批拒绝（任务 → blocked） |
| `/api/tasks/{id}/resources` | PUT | JWT+成员 | 补充资源信息 |
| `/api/tasks/{id}/ack-failure-boundary` | POST | JWT+成员 | 确认知晓失败边界 |
| `/api/tasks/{id}/start` | POST | JWT+成员 | 启动任务 |

**结果与知识发布**：
| 端点 | 方法 | 权限 | 用途 |
|---|---|---|---|
| `/api/results/{id}` | GET | JWT+成员 | 结果详情 |
| `/api/results/{id}/publish` | POST | JWT+PI/Lead | 发布知识（5 态 + 失败边界卡） |
| `/api/results/{id}/freeze` | POST | JWT+PI/Lead | 冻结（版本不匹配） |

**实验护照与会前研讨**：
| 端点 | 方法 | 权限 | 用途 |
|---|---|---|---|
| `/api/experiments/{id}/passport` | GET | JWT+成员 | 实验护照（按会议/实验组织数据链+时间线） |
| `/api/experiments/{id}/brief` | GET | JWT+成员 | 会前研讨包（当前上下文） |

**可信问答**：
| 端点 | 方法 | 权限 | 用途 |
|---|---|---|---|
| `/api/qa/ask` | POST | JWT+实验成员 | 同步问答（9 步检索） |
| `/api/qa/ask/stream` | POST | JWT+实验成员 | 流式 SSE 问答（按阶段 emit） |
| `/api/qa/sessions` | GET | JWT | 会话列表 |
| `/api/qa/sessions/{id}` | GET | JWT+owner | 会话详情（含消息历史） |
| `/api/qa/sessions/{id}` | PATCH | JWT+owner | 编辑会话标题/归档 |
| `/api/qa/sessions/{id}` | DELETE | JWT+owner | 删除会话（CASCADE 清消息） |

**实验与项目管理**：
| 端点 | 方法 | 权限 | 用途 |
|---|---|---|---|
| `/api/experiments` | POST | JWT+PI | 创建实验（仅 PI） |
| `/api/experiments/{id}` | GET | JWT+成员 | 实验详情 |
| `/api/projects` | POST | JWT+PI | 创建项目 |

**控制塔与管理员**：
| 端点 | 方法 | 权限 | 用途 |
|---|---|---|---|
| `/api/control-tower` | GET | JWT+PI/Admin | 全局看板（5 类视图） |
| `/api/admin/reset-demo` | POST | JWT+Admin | 重置演示数据（限速 + 限 Admin） |
| `/api/admin/users` | POST | JWT+Admin | 创建用户 |

##### 3.11.3 飞书 Aily MCP 端点（`/mcp/*`，Bearer 或 queryParam）

| 端点 | 方法 | 鉴权 | 用途 |
|---|---|---|---|
| `/mcp/sse` | GET | Bearer 或 `?token=` | SSE 握手（建立 server→client 流） |
| `/mcp/messages` | POST | session_id 路由 | Aily 发送 client→server 消息 |
| `/mcp/manifest` | GET | Bearer | 工具清单自检 |

---

#### 3.12 安全模型与权限矩阵

##### 3.12.1 双层鉴权

```
┌─────────────────────────────────────────────────────────┐
│ 机器鉴权（PLATFORM_API_KEY）                            │
│   · 编排器 → 平台 /api/v1/*                            │
│   · Aily MCP → /mcp/*                                  │
│   · HMAC-SHA256 常量时间比较（防时序攻击）              │
│   · 双接受 Authorization Bearer + X-Platform-Api-Key   │
├─────────────────────────────────────────────────────────┤
│ 用户鉴权（JWT + bcrypt）                                │
│   · 前端登录 → POST /api/auth/login → access_token     │
│   · JWT 签名（HS256）+ 过期时间                        │
│   · 同 (用户名, IP) 5 次失败锁 15 分钟（429）          │
└─────────────────────────────────────────────────────────┘
```

##### 3.12.2 角色 × 资源 权限矩阵

| 操作 | Admin | PI（全局） | PI（成员） | Lead（成员） | Executor（成员） |
|---|---|---|---|---|---|
| 看全部实验 | ✅ | ✅ | ❌（仅成员） | ❌（仅成员） | ❌（仅成员） |
| 创建项目/实验 | ✅ | ✅ | ❌ | ❌ | ❌ |
| 加入实验成员 | ✅ | ✅ | ❌ | ❌ | ❌ |
| 复核会议 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 任务审批 approve/reject | ✅ | ✅ | ✅ | ✅ | ❌ |
| 任务启动/补充资源/ack 失败边界 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 行动前审计 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 发布知识 / 冻结结果 | ✅ | ✅ | ✅ | ✅ | ❌ |
| 重置演示数据 | ✅ | ❌ | ❌ | ❌ | ❌ |
| 创建用户 | ✅ | ❌ | ❌ | ❌ | ❌ |
| 看控制塔全局看板 | ✅ | ✅ | ❌ | ❌ | ❌ |
| 看实验护照 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 问答 | ✅ | ✅ | ✅ | ✅ | ✅（仅可见实验） |

**关键设计点**：
- 平台 `global_role` 是登录默认身份，**业务权限以 `ExperimentMember.role` 为准**。
- Admin 全局可见但不"自动是 PI"——`ensure_experiment_member` 中 Admin 自动视为成员。
- Executor 不能发布知识（防止误操作污染可信决策）。

##### 3.12.3 攻击面与防御

| 攻击面 | 防御 |
|---|---|
| Webhook 篡改 | HMAC-SHA256 验签 + 5 分钟时间窗防重放 |
| 机器鉴权爆破 | `hmac.compare_digest` 常量时间比较 + 启动告警 |
| 登录爆破 | 同 (用户名, IP) 5 次失败锁 15 分钟 + 429 |
| Webhook body DoS | 1MB 请求体上限 + 413 |
| Webhook 加密推送 | AES 解密，依赖缺失显式报错不静默 |
| 跨实验越权读 | `ensure_experiment_member` 在每个 GET/POST 都校验 |
| MCP 客户端身份伪造 | 不接受 `x-aily-user` 自报头（机器对机器，PLATFORM_API_KEY 唯一凭证） |
| 事件重放 | O_CREAT\|O_EXCL 原子幂等 + 5 分钟时间窗 |
| LLM 输出越权 | Aily 强制 `status=candidate`、`needs_review=true`；真实状态由规则引擎决定 |
| 静默失败 | pipeline 失败 → 发告警卡 + 释放幂等 + 置 FAILED 状态；卡片回调未过审计不静默 success |
| 生产 API 文档泄露 | `APP_ENV=production` 关闭 `/docs` `/redoc` `/openapi.json` |
| CORS 误配 | 白名单（禁 `*` + credentials 组合） |
| MIME 嗅探 / 点击劫持 | nginx 安全响应头：`nosniff` + `SAMEORIGIN` + `CSP frame-ancestors 'self'`（含静态资源路径） |
| 公网入口滥用 | nginx 每 IP 10r/s 限流（burst 40）+ 请求体 5m 上限；敏感路径（`.env`/`.git`/CMS 扫描目标）直接 404 |

---

#### 3.13 前端架构（React 19 + Vite 6 + Tailwind 4）

**核心目录**（`labmemory-platform/frontend/src/`）：

```
src/
├── api/                # API 客户端（fetch 封装 + JWT 注入）
├── components/
│   ├── meeting/        # 复核界面（候选人/证据/版本对比）
│   ├── task/           # 任务审计界面（五项检查 + 一键修正）
│   ├── result/         # 结果发布界面（5 态选择 + 失败边界卡编辑）
│   ├── passport/       # 实验护照（时间线 + 会议链）
│   ├── qa/             # 可信问答（流式 SSE 渲染）
│   └── admin/          # 控制塔 + 演示重置
├── pages/              # 路由页（React Router）
├── hooks/              # 自定义 hooks（useAuth/useStreamQA/...）
├── stores/             # 状态管理（Zustand）
└── lib/                # SSE 客户端 / 引用渲染
```

**关键技术点**：
- **流式 SSE 渲染**：基于 `fetch` + `ReadableStream` 解析 `event: ` + `data: ` 双行格式，前端按事件名切换文案 + 流式追加答案（与 `qa.py::ask_stream_events` 一一对应）。
- **JWT 注入**：axios/fetch 拦截器自动加 `Authorization: Bearer <token>`。
- **状态管理**：Zustand（轻量）+ React Context（用户/实验上下文）。
- **样式**：Tailwind 4 + CSS Variables（深色模式支持）。
- **构建产物**：`frontend/dist/`（index.html + 38KB CSS + 345KB JS），挂载在 FastAPI 静态路径下。

---

### 4. 方案价值

#### 4.1 直接业务价值（脱敏模拟数据）

> ⚠️ **诚实声明**：以下指标均来自脱敏模拟数据与评测样例，不代表晶泰科技真实业务收益。

| 指标 | 原型目标 | 样例结果 | 业务换算 |
|---|---|---|---|
| 过期参数拦截率 | ≥90% | **100%** | 每次拦截 = 避免一轮机器人机时（数小时~一天）+ 稀有物料 + 数天等待周期；按每周 1 次拦截保守估算，一年挽回的实验产能相当可观 |
| 人工整理时间降低 | ≥50% | **56%** | PI 一周节省 5-8 小时（按 30 分钟/会 × 8 场/周估算） |
| 24h 知识发布率 | ≥90% | **92%** | 实验结论当日入知识库，新人第二日即可检索 |
| 问答可追溯率 | 100% | **100%** | 每个答案可回到会议原话与证据；不再有"为什么用这个参数"的扯皮 |
| 人工修改率 | ≤20% | **10%** | Aily 抽取一次到位，PI/Lead 复核修改率低 |
| 冲突检测 Precision/F1 | ≥90% / ≥0.87 | 100% / 0.89 | 数值冲突无漏报 |

#### 4.2 隐性价值（更长期）

1. **新人 onboarding 加速**：新成员通过实验护照即可了解"为什么用 70℃ 而非 80℃"，无需翻聊天记录。
2. **跨实验知识迁移**：失败边界卡 + 复验任务使失败经验可复用，避免下次实验犯同样错。
3. **合规与可审计**：所有决策可追溯到会议原话、参数版本、责任人、证据，符合医药/材料行业的合规要求。
4. **AI 治理示范**：通过"AI 出候选 + 规则定生效"的分工，为企业级 AI 应用提供可复制的治理范式。

#### 4.3 待改进项与诚实声明

我们**没有把方案包装成"全满分"**，而是主动声明未达标项与改进计划：

- **证据关联覆盖率 87.5%**（目标 ≥90%）——计划通过证据锚点抽取的 few-shot 增强在下一迭代补齐。
- **冲突检测 Recall 80%**（目标 ≥85%）——计划通过冲突判定规则扩展（语义冲突、证据冲突）补齐。

这是工程团队对真实性的承诺，也是与"PPT 方案"的本质差异。

---

### 5. 方案体验入口 & Demo 展示视频

#### 5.1 在线体验入口（已部署，公网可访问）

- **地址**：**https://<your-domain.com>**（HTTPS，阿里云 <SERVER_IP> 部署，nginx 反代 → 平台 :8081）
- **演示账号**（密码均为 `123456`）：
  - `pi`（首席研究员视角，可看全部实验）
  - `lead`（项目负责人视角，仅看成员实验）
  - `executor`（执行者视角，仅看成员实验，无发布权限）
  - `admin`（管理员，全局）
- **建议体验路径**：
  1. 用 `pi` 登录 → 决策收件箱（复核候选）→ 主张与版本链
  2. → 任务行动前审计（看一次"阻断"演示）→ 一键修正为当前版本
  3. → 结果回流（看一次 `partially_supported`）→ 失败边界卡
  4. → 实验护照 → 可信问答（试一次"上次说的 80℃ 是什么意思"看指代消解）
- **一键重置演示数据**：管理员账号登录后访问 `/api/admin/reset-demo` 或联系队员远程重置（已在 `scripts/reset_demo.py` 提供）。
- **可用性保障**：服务由 systemd 托管（进程崩溃 5 秒内自动拉起，已实测演练）；数据库每日自动备份（保留 7 份滚动）。

#### 5.2 Demo 展示视频（3-5 分钟）

**视频内容建议**（请录屏时覆盖）：
1. **开场 30 秒**：团队介绍 + 方案一句话定位
2. **2 分钟**：完整跑一遍 Demo 1「旧参数拦截」——从 80℃ 任务创建 → 行动前审计阻断 → 一键修正 → 创建飞书任务
3. **2 分钟**：完整跑一遍 Demo 2「结果回流」——65℃ 提交结果 → `partially_supported` → 失败边界卡 → 复验任务
4. **30 秒**：可信问答演示（指代消解 + 引用回溯）

**视频链接**：【待填写：飞书会议妙记 / Bilibili 等公开链接】

---

## 三、自由展示区（加分项）

### A. 团队成员深度介绍

#### 王健俊（队长 / 产品与可信记忆）

- 负责"主张—证据—版本—状态—适用范围—责任"的可信记忆模型设计
- 负责 PRD V1.0 与评测口径（11 项指标）设计
- 负责"AI 与规则分工"的产品哲学定义——这是 LabMemory 区别于其他 AI 决策工具的核心理念
- 负责 Demo 故事（Demo 1 旧参数拦截、Demo 2 结果回流）的产品化设计

#### 韩广宁（飞书侧编排与 AI 接入）

- 完成飞书 Aily MCP Server 全栈实现（SSE 传输 + Bearer 鉴权 + 10 个工具 + 5 个 Webhook）
- 完成 Aily Skill 提示词设计（10 步可信决策流程）
- 完成飞书适配器全栈（妙记/卡片/任务/多维表格/知识库）
- 完成 38 项集成测试 + 11 项模块体检 + webhook 验签 7 类测试
- 完成可靠性工程（O_CREAT|O_EXCL 原子幂等、按 HTTP code 分类重试、事件乱序容忍）

#### 林俊衡（自研平台与可信决策系统）

- 完成可信决策内核（六道闸门、冲突检测、语义栅栏、参数级版本、5 态知识、三值留痕）
- 完成混合检索 RAG（BM25 + 向量 + 关系扩展 + 重排）+ 流式 SSE
- 完成 FastAPI + React 19 + Vite 6 + SQLAlchemy 全栈
- 完成三轮对抗审查，修复 ~40+ 条缺陷
- 完成阿里云部署（`https://<your-domain.com>`，HTTPS + nginx 反代 + systemd 崩溃自动恢复 + 每日备份）

### B. 技术架构详解

#### B.1 双子系统 + 冻结契约

- **飞书编排子系统**（`feishu-orchestrator`，Python）：事件路由 + Aily 适配器 + 飞书适配器 + 可靠性层
- **可信决策平台**（`labmemory-platform`，FastAPI + React + SQLAlchemy）：六道闸门 + 状态机 + RAG + 护照
- **接口契约**（`openspec/specs/contracts/spec.md`）：4 条版本化冻结的 HTTP 路由 + Bearer 鉴权，支持两侧 Mock 并行开发

#### B.2 双向联动（已打通）

- **正向**（飞书→平台）：`scripts/cross_side_check.py` 5/5 通过（真跨侧直打平台 4 条契约路由）
- **反向**（平台→飞书）：`scripts/reverse_linkage_check.py` 7/7 通过（mock 级通路，真飞书需配真实凭证切 `RUN_MODE=real`）

#### B.3 鉴权与安全设计

- **机器鉴权**：`PLATFORM_API_KEY`（48 位强随机串）支持 `Authorization: Bearer` 与 `X-Platform-Api-Key` 双接受
- **用户鉴权**：JWT + 登录限速（同 (用户名,IP) 5 次失败锁 15 分钟，429）
- **Webhook 验签**：HMAC-SHA256 + 常量时间比较 + 时间戳防重放
- **CORS 白名单**：禁 `*` + credentials 组合
- **生产环境**：关闭 `/docs` / `/redoc` / `/openapi.json`（`APP_ENV=production` 触发）
- **入口层加固**：nginx 全路径安全响应头（`nosniff` / `SAMEORIGIN` / `CSP frame-ancestors`）+ 每 IP 限流 + 请求体上限
- **可用性**：systemd 进程监督（`kill -9` 后 5 秒内自动恢复，已演练）+ SQLite 每日在线备份（保留 7 份滚动）

### C. 工程质量证据（全部可复现）

| 闸门 | 结果 | 复现命令 |
|---|---|---|
| 平台 17 步 e2e 决策全链路 | ✅ 全过 | `python -m scripts.e2e_test` |
| 平台 pytest 单测 | ✅ 通过 | `pytest tests/ -q` |
| 真跨侧联动 cross_side_check | ✅ 5/5 | `python -m scripts.cross_side_check` |
| 反向联动 reverse_linkage_check | ✅ 7/7 | `python -m scripts.reverse_linkage_check` |
| 编排器集成测试 | ✅ 38/38（7 大类） | `python tests/test_integration.py` |
| 编排器模块体检 | ✅ 11/11 | `python scripts/health_check.py` |
| OpenSpec 规格校验 | ✅ 17/17，活跃 change 归零 | `openspec validate --all` |
| Aily MCP 工具清单自检 | ✅ 10 个工具就绪 | `GET /mcp/manifest` |
| 生产部署加固回归（2026-08-16） | ✅ 安全响应头 / kill -9 自动恢复 / 备份完整性 全过 | 公网 `curl -I` + `systemctl` + 备份 `integrity_check` |

### D. 三轮对抗审查的方法论

我们**没有只做"能跑的 demo"**，而是用对抗性审查 + 第一性原理审查主动攻击自己的系统：

- **第一轮（Tier 1）**：~28 条有界缺陷全修——安全越权、迁移列错位、编排器 real 模式契约对齐、幂等 TOCTOU、审计未过静默 success 等。
- **第二轮（Tier 2）**：8 个核心卖点从「PPT/缩水」转为代码实现——六道闸门/冲突检测/语义栅栏/知识 5 态/证据降级/三值留痕/参数级版本/pipeline 诚实 status。
- **第三轮（Round 3）**：可信内核 P0 修复——候选 scope 贯通落库、supersede 按 scope 等价匹配、语义栅栏扫描复核修改值、复核确认/审计写路径纳入实验写锁、任务启动前复查绑定主张版本时效、claim 替代/卡片发布后检索索引同步；QA 多轮指代追问 LLM 改写；编排器可靠性强化；平台登录限速 + 生产环境关闭文档。

每一条都走 OpenSpec change 归档留痕，形成 `AUDIT.md` 中"卖点实现度"对照表——**每个宣称的能力都能指到具体代码行**。

### E. 未来迭代规划

1. **真飞书实时联调**：配置真实企业凭证（`FEISHU_APP_ID/SECRET` + `AILY_API_KEY`），切 `RUN_MODE=real`，完成妙记事件→任务创建的真实闭环验收。
2. **证据 URL 探活**：接入飞书文档权限体系，对证据锚点做周期性可达性校验，失效自动降级主张可信度。
3. **指标补齐**：证据关联覆盖率（few-shot 增强）与冲突召回率（规则扩展）的改进。
4. **规模化**：多 worker 分布式幂等（DB 唯一约束）、`candidate_id` 索引列（QA 会话列表 N+1 已于赛前优化为单条聚合查询）。
5. **推广潜力**：架构与"实验"领域解耦——六道闸门、版本链、审计三态可迁移到任何"会议决策→执行→验证"场景（临床方案评审、工程项目变更、采购决策、合规审计等），具备跨行业复制潜力。

### F. 创新点专题：为什么"AI 出候选 + 规则定生效"是企业 AI 治理的可复制范式

当前企业级 AI 应用普遍存在三类痛点：

1. **AI 直接给结论**——AI 把"可以试试"当真、把幻觉当事实、错误关联实验编号 → 直接采信污染记录
2. **AI 输出缺少版本与审计**——同一个问题不同时间问出不同答案，无法追溯为什么变了
3. **AI 与确定性规则混杂**——想审计"为什么这个参数生效"找不到代码，找代码全是 LLM 调用

LabMemory 的解法是**清晰边界 + 可信内核**：

```
┌────────────────────────────┐
│ Aily（LLM）                │  做它擅长的事：长文本语义理解
│  · 从妙记抽取候选           │  · 标证据锚点（speaker/start/end/text）
│  · 生成结构化 JSON          │  · 给 confidence
└──────────────┬─────────────┘
               │ 候选 JSON（status="candidate", needs_review=true）
               ▼
┌────────────────────────────┐
│ 确定性规则引擎              │  做它擅长的事：编号/数值/版本/权限
│  · experiment_ref 格式校验  │  · 语义栅栏（7 个 hedge 关键词）
│  · 六道闸门（6 类规则）      │  · 冲突检测（数值/范围/语义/证据/责任/版本）
│  · 参数级版本               │  · 三值留痕（原始/AI/人工）
└──────────────┬─────────────┘
               │ 发布决策（passed/needs_confirmation/blocked）
               ▼
┌────────────────────────────┐
│ 人在环                     │  PI/Lead 复核 + 责任归属
│  · 平台复核界面             │  · 飞书卡片 approve/reject/revise
│  · 一键修正                 │  · 失败边界卡显式标注"假设"
└────────────────────────────┘
```

**这套范式可以迁移到任何"长文本会议→结构化决策→执行验证"的场景**——临床方案评审、工程项目变更、采购决策、合规审计。LabMemory 不只是工具，更是企业 AI 治理的方法论。

---

## 四、附录

### 附录 1：幕后故事和花絮

#### 1.1 三人如何分工两个子系统并行开发

我们用 OpenSpec 冻结接口契约（`openspec/specs/contracts/spec.md`）后，三人可以并行开发：

- **王健俊**：专注于 PRD 设计、模型定义、评测口径设计——不写代码但定义"什么是对的"。
- **韩广宁**：飞书侧，从妙记事件接入开始，向 Aily 适配器、再到飞书适配器——纯 Python 子系统。
- **林俊衡**：平台侧，从 DB 模式开始，到 FastAPI 路由、再到 React UI——独立子系统。

两侧用 Mock 并行联调，**契约冻结后没有发生"我等你"或"我猜你怎么做"的阻塞**。合并时 fast-forward 零冲突。

#### 1.2 对抗审查中发现的"AI 把'可以试试'当真"

最有意思的发现：种子数据里有条 `"温度 80℃ 暂定"`，Aily 直接把它分类为 `parameter_change`，复核确认就变成了 current 主张——这正是 PRD line 854 要防的"试探性表达污染"。

修复方案：在 `trust_rules.py` 加 `SEMANTIC_HEDGE_KEYWORDS = ["可以试试", "建议", "可能", "暂定", "试试", "或许", "考虑"]`，扫描候选原文与复核修改值的合并文本，命中即降级为 `pending_validation`，**不让 AI 决定"什么生效"**。

#### 1.3 部署到阿里云时国内镜像源的踩坑

服务器连 PyPI/npm 官方源会卡死（pip 进程 0% CPU 干等）：

- pip 必须用 `https://mirrors.aliyun.com/pypi/simple/`
- npm 必须用 `--registry=https://registry.npmmirror.com`

另一坑：提交的 `frontend/package-lock.json` 内部不一致，导致 `npm install` 在宿主机与干净 `node:22-alpine` 容器里都崩（`Exit handler never called!`）。早先误判为"宿主机 npm 不可用"，**实际是 lockfile 问题**——删 lockfile 重装再生，`npm run build` 正常。

#### 1.4 飞书 Aily 后台"无法配 Bearer Header"的现实

Aily 后台 "添加自定义 MCP" 只支持 name + url + desc 三项，**没有 Authorization Header 配置入口**（一年多内部反馈未支持）。

官方默许的兼容方式是把鉴权信息拼进 URL queryParam（参考高德 MCP `https://mcp.amap.com/sse?key=YOUR_KEY`，与本服务同模式）。

我们同时支持 `?token=` 与 `Authorization: Bearer` 两种鉴权——Aily 后台 URL 校验拒绝带 `?token=` 时还有"无鉴权模式 + nginx IP rate limit"的备用方案。

### 附录 2：参考项目 & 文献

- **OpenSpec**（https://openspec.dev）：规格驱动开发框架，让 AI 编程助手的产出可被规格约束。
- **MCP**（Model Context Protocol）：Anthropic 提出的标准协议，让 LLM 与外部工具解耦。
- **Benchling** 等 ELN/LIMS 行业方案：作为"不替代对象"的参照——LabMemory 专注做 ELN/LIMS 缺失的"会议决策记忆层"。
- **Knowledge Graph / Decision Support Systems**：决策支持系统的经典文献，启发我们把"知识状态"从二元升级为五态。
- **RAG（Retrieval-Augmented Generation）**：混合检索范式（BM25 + 向量 + 重排）的标准实践。
- **Pydantic + FastAPI**：类型驱动的 API 设计，让契约可被代码自动校验。
- **MCP 官方 Python SDK / FastMCP**：FastMCP 1.0 已并入官方 SDK——我们的 MCP Server 直接构建在官方 SDK 之上，与生态同源（赛前调研确认无需引入额外框架）。
- **h5bp/server-configs-nginx**：nginx 安全响应头社区范式，赛前加固（nosniff / X-Frame-Options / CSP frame-ancestors）参考其标准实践。

### 附录 3：交付清单

#### 已交付（✅）

- [x] PRD V1.0（`LabMemory晶研智流_PRD_V1.0.docx`）
- [x] 两份角色说明（飞书侧 + 平台侧）
- [x] 模拟数据与评测样例（`LabMemory模拟数据与评测样例.xlsx`，13 张表）
- [x] 海选演示 HTML 原型（`附件/index.html`）
- [x] 真实 Aily Skill 提示词（`docs/aily-skill/skill-prompt.md`）
- [x] MCP Server（10 工具 + 5 Webhook，部署在 `https://<your-domain.com>/mcp/sse`）
- [x] 状态机 / 六道闸门 / 行动审计
- [x] 飞书真实联动（mock 级通路 + real 模式预留）
- [x] Docker 化 / 一键启动脚本
- [x] 两套 Demo 数据 seed（`scripts/seed_demo.py`）
- [x] 17 个 OpenSpec 领域规格 + 30+ 归档 change
- [x] 阿里云公网部署（`https://<your-domain.com>`，HTTPS）
- [x] 生产部署加固（nginx 安全响应头 + systemd 崩溃自动恢复 + SQLite 每日备份，2026-08-16，change `harden-deployment-and-perf`，脚本固化于 `deploy/`）

#### 待补全（🚧）

- [ ] 真飞书实时联调（需真企业凭证，本次 mock 级通路已验证）
- [ ] 证据 URL 探活（结构化校验已做，URL 探活为下一迭代）
- [ ] 多 worker 分布式幂等（本次单进程原子文件锁；DB 唯一约束为生产增强）

### 附录 4：技术栈与依赖

#### 飞书侧（feishu-orchestrator）

- Python 3.11+
- python-dotenv（环境变量）
- 飞书开放平台 SDK（mock/real 切换）
- 飞书 Aily CLI / HTTP API

#### 平台侧（labmemory-platform）

- 后端：Python 3.12+ / FastAPI / SQLAlchemy / SQLite（可切 PostgreSQL）/ Pydantic
- 前端：React 19 / Vite 6 / Tailwind 4 / TypeScript
- 检索：BM25（自实现）+ 向量（嵌入服务）/ 关系扩展 / 重排
- LLM：DeepSeek-V4-Flash（可降级到模板）

#### 接口契约

- 4 条 HTTP 路由 + Bearer 鉴权
- MCP 协议（SSE 传输）
- Webhook（HMAC-SHA256 签名）

#### 部署

- 阿里云轻量服务器（Ubuntu 24.04，2 核 2G）
- nginx 反代 + let'sencrypt HTTPS（全路径安全响应头 + 每 IP 限流）
- systemd 托管（Restart=always 崩溃自动拉起）；crontab 每日 SQLite 在线备份
- 国内镜像源（PyPI aliyun + npm npmmirror）

---

## 📋 提交前检查清单（提交时删除本节）

- [ ] 补填：Demo 视频链接（信息卡 & §5.2）
- [ ] 补填：个人详细介绍（信息卡，三位成员可补充个人头像/学历/参赛经历）
- [ ] 插入截图：决策收件箱界面、审计阻断卡片、实验护照时间线、Aily MCP 工具清单、`<your-domain.com>` 首页
- [ ] 确认体验入口 https://<your-domain.com> 可正常访问、四个账号可登录（互联网可达）
- [ ] 删除"使用说明"与"提交前检查清单"两节
- [ ] 开启飞书文档【互联网获取链接可阅读权限】
- [ ] 在"AI先锋未来人才大赛-完整参赛方案提交表"提交文档链接

---

**LabMemory（晶研智流）—— 让每条参数有证据，让每次执行用对版本，让每个结果反向校正知识。**

> 提交日期：2026-08-16